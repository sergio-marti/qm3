from distutils.core import setup, Extension

setup( 
	name = "Long Electrostatic corrections for QM/MM calculations", 
	ext_modules = [ 
		Extension( "_long_elec", [ "qm3/engines/long_elec.c" ] )
	]
)
